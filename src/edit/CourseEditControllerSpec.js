'use strict';
require('should');

describe('CourseEditController', function () {
  var $scope, $controller, $routeParams, Course;

  // Set up mocks
  before(function () {
    $scope = {};
    $routeParams = {};
    Course = {};
    $controller = function () {};
  });

  describe('#initFormula', function () {
    // no test...
  });
});
